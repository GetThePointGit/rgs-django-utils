from rgs_django_utils.database.dj_extended_models import TableSection

section_enum_base = TableSection("enum_base", "enum basis", 500)

section_enum_task = TableSection("enum_task", "enum taak", 600)

section_enum_auth = TableSection("enum_auth", "enum authenticatie en autor.", 610)
